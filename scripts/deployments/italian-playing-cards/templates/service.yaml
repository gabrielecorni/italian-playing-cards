apiVersion: v1
kind: Service
metadata:
  name: {{ include "italian-playing-cards.fullname" . }}
  labels:
    {{- include "italian-playing-cards.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "italian-playing-cards.selectorLabels" . | nindent 4 }}
